apiVersion: v1
kind: Service
metadata:
  name: {{ include "metatoc-ui.fullname" . }}
  labels:
    {{- include "metatoc-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "metatoc-ui.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metatoc-ui.fullname" . }}-test-connection"
  labels:
    {{- include "metatoc-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "metatoc-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
